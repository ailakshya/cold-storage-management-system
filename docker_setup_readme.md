# Docker Setup for Local Development

## 1. Dockerfile.local
A multi-stage Dockerfile to build and run the application.

## 2. docker-compose.yml
Orchestrates the Application, TimescaleDB, and Redis.

## Instructions
Run `docker-compose up --build` to start everything.
